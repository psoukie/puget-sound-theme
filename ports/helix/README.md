# Puget Sound for Helix

Copy the Morning and Evening TOML files into your Helix `themes/` directory, then select one in `config.toml`:

```toml
theme = "puget_sound_morning"
# or: theme = "puget_sound_evening"
```
